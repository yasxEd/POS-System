import 'package:flutter/material.dart';

class TipCompletionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('POS Terminal - Tip Completion')),
      body: Center(
        child: Text('Complete tip processing here'),
      ),
    );
  }
}
